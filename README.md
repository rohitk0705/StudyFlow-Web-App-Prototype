# StudyFlow Web App Prototype

This repository contains the StudyFlow Web App Prototype built with Vite, React, and Tailwind CSS.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
